{
  "name": "afford",
  "version": "1.0.0",
  "private": true,
  "description": "Backend assessment — run scripts from here or from ROLL_NUMBER/",
  "scripts": {
    "install:all": "npm install --prefix ROLL_NUMBER && npm run install:all --prefix ROLL_NUMBER",
    "setup": "npm run setup --prefix ROLL_NUMBER",
    "register": "npm run setup --prefix ROLL_NUMBER",
    "dev": "npm run dev --prefix ROLL_NUMBER"
  }
}
